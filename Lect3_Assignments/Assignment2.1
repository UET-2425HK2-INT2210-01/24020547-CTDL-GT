#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <math.h>
#include <iomanip>
#include <set>
using namespace std;

int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n;
    vector<long long> v(n + 1);
    v[0] = 1;
    for (int i = 1; i <= n; ++i) {
        v[i] = v[i-1]*2;
    }
    if (n < 0) {
        cout << 1 / v[n];
    }
    else {
        cout << v[n];
    }
