algorithm ans(n)
  input n
  output 2^n
  Cách 1:
  if n = 0 then return 1;
  half = ans(n/2);
  if n%2=0 then 
  return half*half;
  if n>2 then 
  return half*half*2;
  else
  return half*half/2;
  Cách 2:
  tmp <- abs(n);
  vector ans[tmp+1];
  ans[0] = 1;
  for i <- 1 to tmp do:
  ans[i] <- ans[i-1]*2;
  if n > 0 then
  return ans[tmp];
  else
  return 1/ans[tmp];

Cách 1: O(logn)
Cách 2: O(n)
